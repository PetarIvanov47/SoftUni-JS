function printMatrix(n) {

    for (let i = 0; i < n; i++) {
        
        let col = [];

        for (let j = 0; j < n; j++){
            col.push(n)
        }

        console.log(col.join(' '));
    }

}

printMatrix(2)
